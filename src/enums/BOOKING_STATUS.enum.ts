enum BOOKING_STATUS {
  unassigned = "unassigned",
  done = "done",
  missed = "missed",
  accepted = "accepted",
  assigned = "assigned",
  cancelled = "cancelled",
  confirmed = "confirmed",
  checkedIn = "checkedIn",
  checkedOut = "checkedOut",
  paid = "paid",
}

export default BOOKING_STATUS;
