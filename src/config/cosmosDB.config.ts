import {
  BOOKINGS_CONTAINER_ID,
  DATABASE_ENDPOINT,
  DATABASE_ID,
  DATABASE_KEY,
} from "../constants/DATABASE_CONFIG_PARAMS";

const cosmosDBConfig = {
  endpoint: DATABASE_ENDPOINT,
  key: DATABASE_KEY,
};

export default cosmosDBConfig;
